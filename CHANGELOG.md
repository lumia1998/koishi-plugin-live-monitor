# Changelog

## 0.2.0-alpha.2 - 2026-07-28

### Added

- 新增主播头像本地缓存；检测到开播时保存头像，统计图优先读取本地文件，仅在头像 URL 变化时更新。

## 0.2.0-alpha.1 - 2026-07-28

### Added

- 新增 `liveMonitorSession` 数据表，按场记录直播起止时间、时长、标题、头像、峰值人气和点赞等数据。
- 新增 `直播打卡`、`直播统计`、`日直播统计`、`周直播统计` 和 `月直播统计` 命令，通过 Puppeteer 生成主播统计图片。
- 新增月度直播打卡日历，使用主播头像作为弱化全幅背景，并按每日直播时长显示四级热度。
- 新增今日、本周、本月直播时长和开播次数总览，以及月度逐日直播时长折线图；纵轴按最长单日时长自动生成四等分刻度。
- 新增可配置的连续未开播确认次数，默认连续两次未开播才结束本场直播。

### Changed

- 跨越午夜的直播按自然日拆分时长，分别计入每天的打卡与统计。

### Fixed

- 无条件注册 `liveMonitorState` 数据表模型，修复数据库服务晚于插件加载时持续提示 `cannot resolve table` 的问题。
- 将未启用数据库的提示延后到首次状态恢复时，避免可选服务尚未完成加载时误报。

## 0.1.17 - 2026-07-28

### Added

- 使用 Koishi 数据库持久化直播起始时间、上次直播状态和提醒时间，插件热重载或 Koishi 重启后可继续累计本场直播时长。
- 添加直播时长持久化和下播卡片的回归测试。

### Changed

- 房间状态标识改为基于规范化 URL 的稳定哈希，修改主播名称或通知频道不再重置计时。
- 下播卡片不再显示右上角的“已下播”角标，保留中央的“直播已结束”状态。

### Fixed

- 规范化批量检测结果和配置项的 URL 后再进行关联，修复末尾斜杠或缺少协议时自动通知不生效的问题。
